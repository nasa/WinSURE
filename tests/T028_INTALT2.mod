LAMBDA = 1E-4;
ALPHA = 36 TO* 3600000 BY 10;
BETA = 36000;
DELTA = 3600;
MU = 1/DELTA;  
SIGMA2 = MU;
1,2 = 4*LAMBDA;
2,3 = 3*LAMBDA;
2,4 = <1/(ALPHA+DELTA), SIGMA2, DELTA/(ALPHA+DELTA)>;
2,20 = <1/(ALPHA+DELTA), SIGMA2, ALPHA/(ALPHA+DELTA)>;
20,2 = <1/BETA, SIGMA2>;
20,3 = 3*LAMBDA;           (* FAULT IS BENIGN, BUT STILL DANGEROUS *)
4,5 = 3*LAMBDA;
5,6 = 2*LAMBDA;
5,7 =  <1/(ALPHA+DELTA), SIGMA2, DELTA/(ALPHA+DELTA)>;
5,50 = <1/(ALPHA+DELTA), SIGMA2, ALPHA/(ALPHA+DELTA)>;
50,5 = <1/BETA, SIGMA2>;
50,6 = 2*LAMBDA;
7,8 = 2*LAMBDA;
